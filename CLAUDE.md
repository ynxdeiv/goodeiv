@AGENTS.md

## Claude Code notes

- `.claude/agents` and `.claude/skills` are symlinks to `.agents/roles` and `.agents/skills`. Always edit under `.agents/`.
- If `.local/CONTEXT.md` exists, it is loaded through `CLAUDE.local.md`.
